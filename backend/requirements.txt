fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pandas>=2.1.0
openpyxl>=3.1.0
python-multipart>=0.0.6
pydantic>=2.0.0
pydantic-settings>=2.0.0
numpy>=1.26.0
yfinance>=0.2.0
nsepython>=2.9
requests>=2.28.0
pdfplumber>=0.10.0
xlrd>=2.0.0
akshare>=1.12.0
finance-datareader>=0.9.0
cassandra-driver>=3.29.0
apscheduler>=3.10.0
python-json-logger>=2.0.0

# ── Optional data providers (install as needed) ───────────────────────────────
# Alpha Vantage  →  no extra package (uses requests directly)
# Polygon.io     →  no extra package (uses requests directly)
# IEX Cloud      →  no extra package (uses requests directly)
# Tradier        →  no extra package (uses requests directly)
# Currencylayer  →  no extra package (uses requests directly)
# Quandl / NDL   →  pip install nasdaqdatalink
# TradingView    →  pip install tvdatafeed
# Interactive Brokers  →  pip install ib_insync
